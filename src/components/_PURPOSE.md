# components/

Shared, "dumb" presentational components only (buttons, cards, modals,
layout primitives). No feature-specific logic, no API calls, no
feature-specific state.

If a component only makes sense inside one feature, it belongs in that
feature's own components/ folder, not here.
