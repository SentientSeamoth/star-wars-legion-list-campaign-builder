{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://legion-app.local/schema/command-card.schema.json",
  "title": "Legion Command Card Library",
  "type": "object",
  "required": ["_meta", "command_cards"],
  "properties": {
    "_meta": { "type": "object" },
    "command_cards": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/command_card" }
    }
  },
  "definitions": {
    "command_card": {
      "type": "object",
      "required": [
        "id", "name", "category", "pips", "units_activated",
        "roster_verified"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
        "name": { "type": "string", "minLength": 1 },
        "category": {
          "type": "string",
          "enum": ["generic", "commander-specific"],
          "description": "generic = usable by any commander in any army (subject to faction_restriction/battle_force_restriction if set); commander-specific = only usable if commander_unit_id's unit is in the army."
        },
        "commander_unit_id": {
          "type": ["string", "null"],
          "description": "References an id in data/units.json (rank: commander or operative). Required (non-null) when category is 'commander-specific'."
        },
        "pips": { "type": "integer", "minimum": 1, "maximum": 4 },
        "units_activated": {
          "type": ["integer", "string"],
          "description": "Number of units the card issues orders to. Some advanced cards use conditional/variable wording instead of a flat integer -- use a short string in that case and flag effect_verified accordingly."
        },
        "unit_activation_restriction": {
          "type": ["string", "null"],
          "description": "e.g. 'trooper units only', 'vehicle units only' -- null if the card can order any eligible unit type."
        },
        "faction_restriction": {
          "type": ["string", "null"],
          "enum": ["empire", "separatist", "rebel", "republic", "mercenary", null]
        },
        "battle_force_restriction": {
          "type": ["string", "null"],
          "description": "e.g. '501st Legion' -- narrower than a faction restriction, tied to a specific Battle Force build."
        },
        "effect_description": { "type": ["string", "null"] },
        "effect_verified": { "type": "boolean" },
        "roster_verified": { "type": "boolean" },
        "roster_source": { "type": "string" },
        "source": { "type": ["string", "null"] },
        "notes": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    }
  }
}
